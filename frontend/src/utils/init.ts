// Global frontend initialization
// - Can be extended with Sentry, feature flags, etc.

// Ensure base CSS variables or fonts loaded if needed
// (Mantine provides normalize via provider; no-op for now.)

export {}; 

